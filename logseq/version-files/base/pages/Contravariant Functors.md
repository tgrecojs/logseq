- ## Type Signatures
	- ## Map
	  `map :: Functor m => (a -> b) -> m a -> m b`
		- `map` takes a functor
			- It's actually constrained to a functor.
		- `map` is a Functor is of typ `m`.
			-
		- type m